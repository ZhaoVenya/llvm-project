// #include "stdio.h"

#define MAX_SIZE 20

// int a_set[MAX_SIZE];

// int get_element(int index) {
//     return (MAX_SIZE - index);
// }


int main(){

    int i = MAX_SIZE;

    if(i>15){
        return 0;
    }else{
        return 1;
    }

    // for (i = 0; i < MAX_SIZE; i++) {
    //     a_set[i] = get_element(i);
    // }

    return 0;
}

