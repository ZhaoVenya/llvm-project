# Day1 开启Day架构
## 1.1 添加Day架构

***一共需要修改两个文件***
**第1个文件**
>在llvm后端架构上添加Day架构
```llvm-project/llvm/include/llvm/TargetParser/Triple.h```文件上添加新的内容(新的内容有注释)：*
```cpp
// 添加一个day架构的llvm后端
enum ArchType {
    UnknownArch,
    ...
    renderscript64, // 64-bit RenderScript
    day,    // 这一行是新添加的
    ve,             // NEC SX-Aurora Vector Engine
    LastArchType = ve
  };

  // ... 
  bool isRISCV32() const { return getArch() == Triple::riscv32; }
  // 返回一个bool值，用于判断是否为day架构
  bool isDay() const { return getArch() == Triple::day; } // 这一行是新添加的

  /// Tests whether the target is 64-bit RISC-V.
  bool isRISCV64() const { return getArch() == Triple::riscv64; }
  // ...

```
**第2个文件**
```llvm-project/llvm/lib/TargetParser/Triple.cpp```文件上添加文件上添加新的内容(新的内容有注释)：
```cpp
// 获取架构类型的名称
StringRef Triple::getArchTypeName(ArchType Kind) {
  switch (Kind) {
  ... 
  case ve:             return "ve";
  case day:            return "day"; // 这一行是新添加的
  case wasm32:         return "wasm32";
  ...
  }
  llvm_unreachable("Invalid ArchType!");
}
...
// 获取架构类型的前缀
StringRef Triple::getArchTypePrefix(ArchType Kind) {
  switch (Kind) {
  default:
    return StringRef();

  case aarch64:
  ...
  case ve:          return "ve";
  case day:         return "day";// 这一行是新添加的
  case csky:        return "csky";
  ...
  }
}
...

static Triple::ObjectFormatType getDefaultFormat(const Triple &T) {
  switch (T.getArch()) {
    case Triple::UnknownArch:
    ...
    case Triple::x86_64:
        switch (T.getOS()) {
        case Triple::Win32:
        case Triple::UEFI:
        return Triple::COFF;
        default:
        return T.isOSDarwin() ? Triple::MachO : Triple::ELF;
        }
    case Triple::aarch64_be:
    ...
    case Triple::ve:
    case Triple::day:// 这一行是新添加的
    case Triple::xcore:
    case Triple::xtensa:
        return Triple::ELF;
    ...
  }
}
...

unsigned Triple::getArchPointerBitWidth(llvm::Triple::ArchType Arch) {
    switch (Arch) {
        case llvm::Triple::UnknownArch:
            return 0;

        case llvm::Triple::avr:
        case llvm::Triple::msp430:
            return 16;

        case llvm::Triple::aarch64_32:
        ...
        case llvm::Triple::xtensa:
        case llvm::Triple::day:// 这一行是新添加的
            return 32;
    }
    ...

    Triple Triple::get32BitArchVariant() const {
        Triple T(*this);

        switch (getArch()) {
        case Triple::UnknownArch:
        ...
        case Triple::ve:
            T.setArch(UnknownArch);
            break;

        case Triple::aarch64_32:
        case Triple::amdil:
        ...
        case Triple::xtensa:
        case Triple::day: // 这一行是新添加的
            // Already 32-bit.
            break;
        }
    return T;
    }
}

...

// day架构不支持大端模式
Triple Triple::getBigEndianArchVariant() const {
  Triple T(*this);
  // Already big endian.
  if (!isLittleEndian())
    return T;
  
    switch (getArch()) {
      case Triple::thumb:
      case Triple::day: // 这一行是新添加的
        T.setArch(UnknownArch);
      break;
      // ...
   }
}

// day架构是小端模式
bool Triple::isLittleEndian() const {
  switch (getArch()) {
  ...
  case Triple::day: // 这一行是新添加的
    return true;
  default:
    return false;
  }
}

```
## 1.2 添加Day架构必要的文件
***一共需要添加8个文件***
添加的8个文件，其目录在```llvm-project/llvm/lib/Target/Day```文件夹下，结构为：
>llvm-project/llvm/lib/Target/Day$ tree
>.
>├── CMakeLists.txt
>├── DayTargetMachine.cpp
>├── MCTargetDesc
>│   ├── CMakeLists.txt
>│   ├── DayMCTargetDesc.cpp
>│   └── DayMCTargetDesc.h
>└── TargetInfo
>    ├── CMakeLists.txt
>    ├── DayTargetInfo.cpp
>    └── DayTargetInfo.h

**第1个文件**
```llvm-project/llvm/lib/Target/Day/CMakeLists.txt```文件，内容是：
```CMake
add_llvm_component_group(Day)

add_subdirectory(MCTargetDesc)
add_subdirectory(TargetInfo)

add_llvm_target(DayCodeGen
  DayTargetMachine.cpp
  
  LINK_COMPONENTS
  Support
)
```
**第2个文件**
```llvm-project/llvm/lib/Target/Day/DayTargetMachine.cpp```文件，内容是：
```cpp
#include "llvm/MC/TargetRegistry.h"

#include "TargetInfo/DayTargetInfo.h"
#define DEBUG_TYPE "day"

extern "C" LLVM_EXTERNAL_VISIBILITY void LLVMInitializeDayTarget() {

}
```

**第3个文件**
```llvm/lib/Target/Day/TargetInfo/CMakeLists.txt```文件，内容是：
```CMake
add_llvm_component_library(LLVMDayInfo
        DayTargetInfo.cpp

        LINK_COMPONENTS
        Support
        MC
)
```

**第4个文件**
```llvm/lib/Target/Day/TargetInfo/DayTargetInfo.cpp```文件，内容是：
```cpp
#include "DayTargetInfo.h"
#include "llvm/MC/TargetRegistry.h"

using namespace llvm;

Target &llvm::getTheDayTarget() {
	static Target TheDayTarget;
	return TheDayTarget;
}

extern "C" LLVM_EXTERNAL_VISIBILITY void LLVMInitializeDayTargetInfo() {
  RegisterTarget<Triple::day,
                 /*HasJIT=*/true>
      X(getTheDayTarget(), "day", "Day (32-bit little endian)", "Day");
}
```
**第5个文件**
```llvm/lib/Target/Day/TargetInfo/DayTargetInfo.h```文件，内容是：
```cpp
#ifndef DAY_TARGET_INFO_H
#define DAY_TARGET_INFO_H

namespace llvm {
    class Target;
    Target &getTheDayTarget();
}

#endif
```

**第6个文件**
```llvm/lib/Target/Day/MCTargetDesc/CMakeLists.txt```文件，内容是：
```CMake
add_llvm_component_library(LLVMDayDesc
        DayMCTargetDesc.cpp

        LINK_COMPONENTS
        Support
        CodeGenTypes
        MC
)
```
**第7个文件**
```llvm/lib/Target/Day/MCTargetDesc/DayMCTargetDesc.cpp```文件，内容是：
```cpp
#include "DayMCTargetDesc.h"

#include "llvm/MC/TargetRegistry.h"

extern "C" LLVM_EXTERNAL_VISIBILITY void LLVMInitializeDayTargetMC() {

}
```

**第8个文件**
```llvm/lib/Target/Day/MCTargetDesc/DayMCTargetDesc.h```文件，内容是：
```cpp
#ifndef DAY_MCTARGETDESC_H
#define DAY_MCTARGETDESC_H

#endif
```

## 1.3 Build工程
在llvm-project工程下，创建一个build文件，并且进行以下操作
```bash
mkdir build
cd build
cmake -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_C_COMPILER=clang -DCMAKE_BUILD_TYPE=Debug -DLLVM_OPTIMIZED_TABLEGEN=ON -DLLVM_TARGETS_TO_BUILD="X86;RISCV" -DLLVM_EXPERIMENTAL_TARGETS_TO_BUILD="Day" -DCMAKE_LINKER=ld.lld -DBUILD_SHARED_LIBS=ON -DLLVM_ENABLE_PROJECTS="clang;lld" -G "Ninja" ../llvm

cmake -DCMAKE_CXX_COMPILER=clang++ -DCMAKE_C_COMPILER=clang -DCMAKE_BUILD_TYPE=Debug -DLLVM_OPTIMIZED_TABLEGEN=ON -DLLVM_TARGETS_TO_BUILD="X86;RISCV;Sparc" -DLLVM_EXPERIMENTAL_TARGETS_TO_BUILD="Day" -DCMAKE_LINKER=ld.lld -DBUILD_SHARED_LIBS=ON -DLLVM_ENABLE_PROJECTS="clang;lld" -G "Ninja" ../llvm
ninja -j10
# 等待构建完成即可
```

# Day2 更新ELF相关
## 2.1 创建def文件
创建```llvm/include/llvm/BinaryFormat/ELFRelocs/Day.def```文件，内容如下：
```cpp
#ifndef ELF_RELOC 
#error "ELF_RELOC must be defined" 
#endif 

ELF_RELOC(R_Day_NONE, 0)
```
## 2.2 更新ELF文件相关
***一共有4个文件***
**第1个文件**
更新```llvm/include/llvm/BinaryFormat/ELF.h```文件，内容如下：
```cpp
enum{
  ...
  EM_VE = 251,            // NEC SX-Aurora VE
  EM_CSKY = 252,          // C-SKY 32-bit processor
  EM_LOONGARCH = 258,     // LoongArch
  EM_DAY = 260,
};
...
// ELF Relocation types for BPF
enum {
#include "ELFRelocs/BPF.def"
};

// ELF Relocation types for RISCVN   //新增代码
enum {                               //新增代码
#include "ELFRelocs/Day.def"         //新增代码
};

// ELF Relocation types for M68k
enum {
#include "ELFRelocs/M68k.def"
};

```
**第2个文件**
更新```llvm/include/llvm/Object/ELFObjectFile.h```文件，内容如下：
```cpp
StringRef ELFObjectFile<ELFT>::getFileFormatName() const {
  constexpr bool IsLittleEndian = ELFT::Endianness == llvm::endianness::little;
  switch (EF.getHeader().e_ident[ELF::EI_CLASS]) {
  case ELF::ELFCLASS32:
    switch (EF.getHeader().e_machine) {
    case ELF::EM_DAY:       // 新增代码
      return "elf32-day";   // 新增代码
    case ELF::EM_68K:
      return "elf32-m68k";
    ...
    }
    ...
  }
}
...
template <class ELFT> Triple::ArchType ELFObjectFile<ELFT>::getArch() const {
  bool IsLittleEndian = ELFT::Endianness == llvm::endianness::little;
  switch (EF.getHeader().e_machine) {
  case ELF::EM_DAY:        // 新增代码
    return Triple::day; // 新增代码
  case ELF::EM_68K:
    return Triple::m68k;
    }
}
```
**第3个文件**
```llvm/lib/Object/ELF.cpp```文件，内容：
```cpp
StringRef llvm::object::getELFRelocationTypeName(uint32_t Machine,
                                                 uint32_t Type) {
  switch (Machine) {
  case ELF::EM_DAY:
    switch (Type) {
#include "llvm/BinaryFormat/ELFRelocs/Day.def"
    default:
      break;
    }
  break;
  }
}
```
**第4个文件**
```llvm/lib/ObjectYAML/ELFYAML.cpp```文件，内容：
```cpp
void ScalarEnumerationTraits<ELFYAML::ELF_EM>::enumeration(
    IO &IO, ELFYAML::ELF_EM &Value) {
#define ECase(X) IO.enumCase(Value, #X, ELF::X)
  ECase(EM_NONE);
  ECase(EM_DAY);// 新增代码
  ECase(EM_M32);
  ...
  }
```
```cpp
void ScalarEnumerationTraits<ELFYAML::ELF_REL>::enumeration(
    IO &IO, ELFYAML::ELF_REL &Value) {
  const auto *Object = static_cast<ELFYAML::Object *>(IO.getContext());
  assert(Object && "The IO context is not initialized");
#define ELF_RELOC(X, Y) IO.enumCase(Value, #X, ELF::X);
  switch (Object->getMachine()) {
  case ELF::EM_DAY:                             // 新增内容
#include "llvm/BinaryFormat/ELFRelocs/Day.def"  // 新增内容
    break;                                      // 新增内容
    }
}
```

# Day3 添加对应的Tablegen文件
***添加必要的tablegen文件***
## 3.1 添加llvm/lib/Target/Day/DayRegisterInfo.td文件
> 详见github上标签Day3对应的内容
## 3.2 添加llvm/lib/Target/Day/DayInstrFormats.td文件
> 详见github上标签Day3对应的内容
## 3.3 添加llvm/lib/Target/Day/DayInstrInfo.td文件
> 详见github上标签Day3对应的内容
## 3.4 添加llvm/lib/Target/Day/DayCallingConv.td文件
> 详见github上标签Day3对应的内容
## 3.5 添加llvm/lib/Target/Day/Day.td文件
> 详见github上标签Day3对应的内容

# Day4 
**添加好前端后,可以生成对应的ll文件**
```bash
 ../../build/bin/clang --target=day-unknown-elf -S -emit-llvm main.c -o bbb.ll
```
```bbb.ll```文件中的内容如下,注意```target datalayout```和```target triple```:
```asm
; ModuleID = 'main.c'
source_filename = "main.c"
target datalayout = "e-m:e-p:32:32-i32:32-n32-S32"
target triple = "day-unknown-unknown-elf"

; Function Attrs: noinline nounwind optnone
define dso_local i32 @main() #0 {
entry:
  %retval = alloca i32, align 4
  store i32 0, ptr %retval, align 4
  ret i32 10
}

attributes #0 = { noinline nounwind optnone "frame-pointer"="all" "no-trapping-math"="true" "stack-protector-buffer-size"="8" }

!llvm.module.flags = !{!0, !1}
!llvm.ident = !{!2}

!0 = !{i32 1, !"wchar_size", i32 4}
!1 = !{i32 7, !"frame-pointer", i32 2}
!2 = !{!"clang version 20.1.7 (https://github.com/ZhaoVenya/llvm-project.git ae9cce1ffe28fa0f708e150833e62f3a79c0d39b)"}
```

