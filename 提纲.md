>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

>>>>>>>>>>


>>>>>>>>>>
从MIR到Machine Code需要经历以下过程：
    1.指令选择
    2.寄存器分配
    3.指令调度
    4.代码发射

但是在具体实现过程中，有很多文件，文件列表如下：
├── CMakeLists.txt
├── MCTargetDesc
│   ├── CMakeLists.txt
│   ├── OneInstPrinter.cpp
│   ├── OneInstPrinter.h
│   ├── OneMCAsmInfo.cpp
│   ├── OneMCAsmInfo.h
│   ├── OneMCTargetDesc.cpp
│   └── OneMCTargetDesc.h
├── OneAsmPrinter.cpp
├── OneAsmPrinter.h
├── OneCallingConv.td
├── OneFrameLowering.cpp
├── OneFrameLowering.h
├── One.h
├── OneInstrFormats.td
├── OneInstrInfo.cpp
├── OneInstrInfo.h
├── OneInstrInfo.td
├── OneISelDAGToDAG.cpp
├── OneISelLowering.cpp
├── OneISelLowering.h
├── OneRegisterInfo.cpp
├── OneRegisterInfo.h
├── OneRegisterInfo.td
├── OneSubtarget.cpp
├── OneSubtarget.h
├── OneTargetMachine.cpp
├── OneTargetMachine.h
├── One.td
└── TargetInfo
    ├── CMakeLists.txt
    ├── OneTargetInfo.cpp
    └── OneTargetInfo.h

这些对应的文件分别是哪些步骤应该用到的, 详见下表：

>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>目标描述
        |One.td, 
        |OneRegisterInfo.td, 
        |OneInstrFormats.td, 
        |OneInstrInfo.td,
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>目标初始化	
        |OneTargetMachine, OneTargetMachine ：CodeGenTargetMachineImpl, 定义目标机器的主要特性，连接所有后端组件
        |OneSubtarget, OneSubtarget ：OneGenSubtargetInfo, 处理特定子目标的特性
        |TargetInfo/OneTargetInfo, 声明Target类，并且根据这个类函数的返回值，注册了One这个架构,实现目标注册和基本信息（如目标名称）
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>指令选择	
        |OneISelLowering, OneTargetLowering ：TargetLowering, 实现从LLVM IR到DAG的 lowering 过程
        |OneISelDAGToDAG, OneDAGToDAGISel ：SelectionDAGISel, 实现从DAG到机器指令的选择
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>寄存器分配与栈帧	
        |OneRegisterInfo, OneRegisterInfo ：OneRegisterInfo, 定义寄存器文件及其特性
        |OneFrameLowering, OneFrameLowering ：TargetFrameLowering, 处理函数栈帧的创建和销毁
        |OneCallingConv, 这里边只是定义了几个相关的函数，但是并没有统一class的名称, 定义调用约定（参数传递、返回值处理等）
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>汇编/二进制生成	
        |OneAsmPrinter, OneAsmPrinter ： AsmPrinter, 实现从LLVM IR到汇编代码的转换
        |MCTargetDesc/ 下的文件, 机器码目标描述 
                OneInstPrinter : MCInstPrinter, 实现指令打印功能，将机器指令转换为可读的汇编格式
                OneMCAsmInfo : MCAsmInfoELF , 定义汇编器的特性，如注释符号、对齐方式等汇编器相关信息
                OneMCExpr ：MCTargetExpr, 实现目标特定的机器码表达式处理
                MCTargetDesc.cpp/h 文件中没有定义新的类, 只是实现了一些对应的函数，注册所有机器码相关的描述信息
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>指令支持	
        |OneInstrInfo, OneInstrInfo : OneGenInstrInfo, 定义指令集特性（编码、延迟等）
        |One.h, 主头文件，包含后端的基本定义和声明
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>


>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>
>>>>  示例：从 IR 到汇编的流程
>>>>  
>>>>      输入 IR：%r = add i32 %a, %b
>>>>  
>>>>      指令选择（OneISelDAGToDAG）：匹配 add → One::ADD 指令。
>>>>  
>>>>      寄存器分配（OneRegisterInfo）：%a → R0, %b → R1, %r → R2。
>>>>  
>>>>      栈帧处理（OneFrameLowering）：必要时调整栈指针。
>>>>  
>>>>      汇编生成（OneAsmPrinter + OneInstPrinter）：输出 ADD R2, R0, R1。
>>>>  
>>>>      MC 层处理（OneMCTargetDesc）：最终生成二进制或文本汇编。
>>>>  
>>>>  通过这种模块化设计，LLVM 后端可以高效地将高级 IR 转换为目标机器码。
>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>>

